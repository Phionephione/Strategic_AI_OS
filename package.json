{
  "name": "strategic-ai-os-root",
  "version": "1.0.0",
  "scripts": {
    "backend": "cd backend && uvicorn main:app --reload",
    "frontend": "cd frontend && npm start",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}